# SPA fallback: client-side routes (e.g. /vehicles, /verify) serve index.html.
# Static assets are served before this rule, and /api/* is handled by the
# Pages Function, so neither is affected.
/*    /index.html   200
