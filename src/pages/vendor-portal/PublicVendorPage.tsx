/**
 * Public, no-auth page at /vendor/:token — a capability link (unguessable token),
 * same posture as /q/:token and /verify. Placeholder; replaced by the module build.
 */
import { LoadingState } from "../../components/ui";

export default function PublicVendorPage() {
  return <LoadingState />;
}
