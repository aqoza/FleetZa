// Driver behavior module (driver_behavior). Arabic lives in ../ar/driverBehavior.ts and must stay complete.
export const enDriverBehavior = {
  "driverBehavior.title": "Driver behavior",
  "driverBehavior.subtitle": "Safety events, driver scorecards and coaching.",
} as const;
